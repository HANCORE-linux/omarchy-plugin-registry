<p>Your <strong>plugins.omarchy.org</strong> sign-in code:</p>
<p style="font-family: monospace; font-size: 28px; letter-spacing: 6px;"><%= @login_code.code %></p>
<p>It expires in 15 minutes. If you didn't request this, ignore this email — nobody can sign in without it.</p>
